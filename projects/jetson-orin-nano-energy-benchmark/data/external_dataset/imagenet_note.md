# External Dataset: ImageNet 2012 Validation Set

The experiments use the ImageNet 2012 validation set with 50,000 images.

The ImageNet image files are not redistributed in this repository. Users must obtain the dataset separately from ImageNet and arrange the validation images in the class-directory structure expected by PyTorch.

The benchmark scripts assume that the dataset is available locally before execution.
